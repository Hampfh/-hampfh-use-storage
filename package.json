{
  "private": true,
  "scripts": {
    "build": "yarn build:lib",
    "build:lib": "yarn workspace @hampfh/use-storage build",
    "link:example": "yarn workspace example link-lib"
  },
  "workspaces": [
    "use-storage",
    "example"
  ]
}
